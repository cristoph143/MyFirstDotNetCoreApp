@model dynamic
@{
    ViewBag.Title = "Options Pattern";
    Layout = "_Layout";
}

@{
    var partialViewData = new ViewDataDictionary(ViewData);
    partialViewData.Remove("Title");
    partialViewData.Add("Title", "Options Pattern");
}

@await Html.PartialAsync("_ConfigurationPartial", partialViewData)